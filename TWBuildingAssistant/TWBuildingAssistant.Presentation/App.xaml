<Application x:Class="TWBuildingAssistant.Presentation.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TWBuildingAssistant.Presentation"
             xmlns:viewmodels="clr-namespace:TWBuildingAssistant.Presentation.ViewModels"
             xmlns:views="clr-namespace:TWBuildingAssistant.Presentation.Views">
    <Application.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type viewmodels:SettingsViewModel}">
                <views:SettingsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:SeekerViewModel}">
                <views:SeekerView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:ProvinceViewModel}">
                <views:ProvinceView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:RegionViewModel}">
                <views:RegionView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:SlotViewModel}">
                <views:SlotView/>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
